<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Web Development for Nonprofits" %>">
    <%= csrf_meta_tags %>

    <title><%= content_for?(:title) ? yield(:title) : "Web Development for Nonprofits" %></title>
    
    <%= stylesheet_link_tag "application", media: "all", "data-turbolinks-track" => true %>
  </head>
  <body>
    <main role="main">
       <%= render 'layouts/messages' %>
       <%= yield %>
    </main>

    <%= javascript_include_tag "application", "data-turbolinks-track" => true %>
  </body>
</html>
